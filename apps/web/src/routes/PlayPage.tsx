import { MilestonePlaceholder } from "./MilestonePlaceholder.js";

export function PlayPage() {
  return (
    <MilestonePlaceholder title="Play" milestone="M4">
      <p>
        Single-device mode: reference video plus the local camera as the pose source. This is the
        first testable version of the game (prototype P1).
      </p>
    </MilestonePlaceholder>
  );
}
